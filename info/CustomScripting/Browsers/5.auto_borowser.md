# 5.auto_borowser.py

**Path:** `CustomScripting/Browsers/5.auto_borowser.py`

**Automation Type:** Browser Automation
**Lines:** 249

## Purpose

------------------------- Logging setup ------------------------- ------------------------- Global variables -------------------------

## Key Features

- Web browser control

## Usage Pattern

Function-based - Provides reusable functions with standalone execution capability

## Dependencies

- `json`
- `os`
- `pickle`
- `logging`
- `requests`
- `selenium.webdriver`
- `selenium.webdriver.chrome.options.Options`
- `selenium.webdriver.chrome.service.Service`
- `os_get_installed_useragent.get_chrome_user_agent`

## Functions

### save_cookies

**Parameters:** driver

Save cookies from the WebDriver instance to a file.

### load_cookies

**Parameters:** driver

Load cookies into the WebDriver instance from a file.

### verify_proxy

**Parameters:** driver_instance, proxy_ip

Verify if the proxy is working by checking the public IP via multiple endpoints.

### test_proxy_connectivity

**Parameters:** proxy_ip, proxy_port

Test if the proxy is functional using HTTP requests.

### load_proxies

**Parameters:** file_path

Load proxies into a list from a specified file.

### lanuch_initialize_driver

Initialize Chrome WebDriver with optional proxy.

### setup_chrome_options

**Parameters:** chrome_options

## External APIs

This script interacts with external services:
- `https://httpbin.org/ip",`
- `https://api.myip.com/",`
- `https://ifconfig.me/ip"`
- `https://"`
- `http://{proxy_ip}:{proxy_port}"`

## Code Examples

### save_cookies

```python
def save_cookies(driver):
    """Save cookies from the WebDriver instance to a file."""
    try:
        if driver is None:
            logging.error("Driver is not initialized. Cannot save cookies.")
            return

        with open(COOKIE_FILE_PATH, 'wb') as file:
            pickle.dump(driv
```

### load_proxies

```python
def load_proxies(file_path):
    """Load proxies into a list from a specified file."""
    global global_proxy_list

    try:
        with open(file_path, "r") as file:
            global_proxy_list = [line.strip().split(":") for line in file.readlines() if line.strip()]
            if not global_pr
```

