# 7.auto_borowser.py

**Path:** `CustomScripting/Browsers/7.auto_borowser.py`

**Automation Type:** Browser Automation
**Lines:** 345

## Purpose

------------------------------------------------- user must have chrome installed on the computer ------------------------------------------------- ------------------------- Logging setup ------------------------- ------------------------- Global variables -------------------------

## Key Features

- Web browser control

## Usage Pattern

Function-based - Provides reusable functions with standalone execution capability

## Dependencies

- `json`
- `os`
- `pickle`
- `logging`
- `selenium.webdriver`
- `time`
- `selenium.webdriver.chrome.options.Options`
- `selenium.webdriver.chrome.service.Service`
- `os_chrome_useragent.get_chrome_user_agent`
- `time`

## Functions

### save_cookies

**Parameters:** driver

Save cookies from the WebDriver instance to a file.

### load_cookies

**Parameters:** driver

Load cookies into the WebDriver instance from a file and check their validity.

### verify_proxy

**Parameters:** driver_instance, proxy_ip

Verify if the proxy is working by checking the public IP.

### load_proxies

**Parameters:** file_path

Helper function to load proxies from file.

### lanuch_initialize_driver

Initialize the Selenium WebDriver with proxy, cookies, and extensions.

### setup_chrome_options

**Parameters:** chrome_options

Sets up Chrome options to customize WebDriver behavior.

## External APIs

This script interacts with external services:
- `https://www.instagram.com/")`
- `https://httpbin.org/ip")`
- `https://instagram.com")`
- `http://{proxy_ip}:{proxy_port}")`
- `https://instagram.com")`

## Code Examples

### save_cookies

```python
def save_cookies(driver):
    """Save cookies from the WebDriver instance to a file."""
    try:
        if driver is None:
            logging.error("Driver is not initialized. Cannot save cookies.")
            return

        with open(COOKIE_FILE_PATH, 'wb') as file:
            pickle.dump(driv
```

### load_proxies

```python
def load_proxies(file_path):
    """Helper function to load proxies from file."""
    global global_proxy_list

    try:
        with open(file_path, "r") as file:
            global_proxy_list = [line.strip().split(":") for line in file.readlines()]
            if not global_proxy_list:
           
```

