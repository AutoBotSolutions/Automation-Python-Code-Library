## Me
using python code an example using return

## AI Assistant
Here is a basic example of using the `return` statement in Python:

### Example: Return Value from a Function

```python
def add_numbers(a, b):
    # This function takes two numbers and returns their sum
    result = a + b
    return result

# Call the function and store the returned value
sum_result = add_numbers(5, 7)

# Print the result
print("The sum is:", sum_result)
```

### Explanation:
1. The function `add_numbers` takes two arguments, `a` and `b`.
2. Inside the function, `result` is calculated as the sum of `a` and `b`.
3. The `return` statement is used to return the result.
4. The returned value is then stored in the variable `sum_result` when the function is called.
5. Finally, `sum_result` is printed. This will display `The sum is: 12`.

