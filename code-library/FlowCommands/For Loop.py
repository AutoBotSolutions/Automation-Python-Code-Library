To loop 100 times in Python, you can use a `for` loop. Here's an example:

```python
# Looping 100 times
for i in range(100):
    print(f"Iteration number: {i+1}")
```

### Explanation:
- The `range(100)` generates numbers from `0` to `99`, so the loop will run exactly 100 times.
- `i+1` is used to display the iteration starting from `1` (optional).

If you need something specific within the loop, please let me know!
