## Me
set variable to 0, build an if then els statement as follows. if said variable = 1 then alert found object, els alert object not found

## AI Assistant
Here’s how you can implement the requested logic in JavaScript:

```
let variable = 0;

if (variable === 1) {
    alert("Found object");
} else {
    alert("Object not found");
}
```

### Explanation:
1. **Initializing the variable**: `variable` is set to `0`.
2. **If condition**: Checks whether `variable` equals `1` (`variable === 1`).
3. **`alert()`**:
   - If `variable` is `1`, it shows the message "Found object".
   - Otherwise, it shows the message "Object not found".

Let me know if you need an example in another programming language!

