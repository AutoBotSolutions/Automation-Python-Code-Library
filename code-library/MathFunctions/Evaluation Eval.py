Here is a simple example in Python to create an if statement evaluating two variables between 1-20, checking if their sum equals 15, and producing alerts accordingly:

```python
# Define two variables with values between 1 and 20
value1 = 10
value2 = 5

# Check if the sum of the two values equals 15
if value1 + value2 == 15:
    print("Great!")  # Alert if the condition is met
else:
    print("Bummer!")  # Alert if the condition is not met
```

### Explanation:
1. Two variables `value1` and `value2` are initialized with values between 1 and 20.
2. The `if` statement evaluates whether the sum of the two variables is exactly 15.
3. If the condition is true, the program outputs `"Great!"`. Otherwise, it outputs `"Bummer!"`.

You can replace `value1` and `value2` with other values within the range [1, 20] to test different cases.
